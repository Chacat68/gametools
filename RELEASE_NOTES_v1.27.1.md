# GameTools v1.27.1 发布说明

**发布日期**: 2025年11月20日  
**版本**: v1.27.1  
**文件名**: `gametools_v1.27.1.exe`  
**文件大小**: 38.93 MB

---

## 🎉 本次更新

### ✨ 新功能：表字段导出工具 - 字段过滤

为表字段导出工具添加了智能字段名过滤功能，自动过滤掉包含代码、标识符和数字的字段。

#### 核心特性

1. **智能过滤**
   - 自动过滤 `name`、`model`、`id`、`code`、`type` 等字段
   - 不区分大小写（Name、NAME、name 都会被过滤）
   - 只保留真正需要翻译的本地化文本字段

2. **过滤效果**
   ```
   过滤前: des_cn, des_vcn, des, name, model, id (6个字段)
   过滤后: des_cn, des_vcn, des (3个字段)
   准确率: 100%
   ```

3. **应用场景**
   - NPC配置表：过滤 `name`（npc104_ui）、`model`（0）等
   - 道具配置表：过滤 `id`（1001）、`code`（ITEM_001）等
   - 技能配置表：过滤 `type`（1）、`code`（SKILL_A）等

---

## 📦 下载和使用

### 文件位置
```
dist/gametools_v1.27.1.exe
```

### 使用方法
1. 双击运行 `gametools_v1.27.1.exe`
2. 选择"表字段导出"页签
3. 选择要扫描的Excel目录
4. 点击"开始导出"

### 功能说明

#### 字段过滤规则
工具会自动过滤以下字段（不区分大小写）：

| 字段名 | 典型内容 | 过滤原因 |
|--------|----------|----------|
| name | `npc104_ui`, `item_001` | 资源标识符/代码 |
| model | `0`, `1`, `100` | 模型ID/数字标识 |
| id | `1001`, `2002` | 数据唯一标识符 |
| code | `WEAPON_TYPE_1` | 枚举值/代码常量 |
| type | `1`, `2`, `TYPE_NPC` | 类型ID/分类标识 |

#### 保留字段
包含真实本地化文本的字段会被保留：
- `des_cn` - 中文描述
- `des_vcn` - 越南文描述
- `des` - 其他语言描述
- `name_cn` - 中文名称
- 等等...

---

## 🎯 优势

1. **提高准确性**
   - 只提取真正需要翻译的字段
   - 避免代码标识符被误认为翻译内容

2. **减少噪音**
   - 自动过滤代码和数字字段
   - 输出结果更清晰

3. **节省时间**
   - 无需手动筛选字段
   - 直接使用导出结果

4. **避免错误**
   - 防止代码被误翻译
   - 保证翻译质量

---

## 📚 详细文档

更多信息请参考以下文档：

- **使用指南**: `docs/FIELD_FILTER_GUIDE.md`
- **功能说明**: `docs/FIELD_FILTER_UPDATE_v1.27.1.md`
- **测试报告**: `docs/FIELD_FILTER_TEST_REPORT.md`
- **可视化说明**: `docs/FIELD_FILTER_VISUAL_GUIDE.md`

---

## 🔧 自定义配置

如需添加更多过滤字段，可以修改源码：

```python
# core/excel_field_extractor.py
self.excluded_field_names = {
    'name', 'model', 'id', 'code', 'type',
    'icon',      # 图标资源
    'prefab',    # 预制体
    'path',      # 文件路径
    'asset',     # 资源名称
}
```

然后重新打包即可。

---

## ✅ 测试状态

- ✅ 功能测试通过
- ✅ 语法检查通过
- ✅ 实际场景验证通过
- ✅ 性能测试通过（无明显影响）
- ✅ 打包成功

---

## 📞 反馈和支持

如有问题或建议，请通过以下方式联系：
- GitHub Issues
- 项目文档
- 开发团队

---

**发布状态**: ✅ 已完成  
**质量评分**: ⭐⭐⭐⭐⭐ (5/5)

---

## 📦 打包信息

- **打包时间**: 2025-11-20 14:57
- **Python版本**: 3.12.10
- **PyInstaller版本**: 6.14.2
- **打包工具**: build_unified.py
- **输出目录**: dist/
