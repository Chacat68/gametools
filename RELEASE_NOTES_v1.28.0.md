# GameTools v1.28.0 发布说明

## 📦 版本信息

- **版本号**: v1.28.0
- **发布日期**: 2025年11月20日
- **文件名**: gametools_v1.28.0.exe
- **文件大小**: 38.94 MB
- **构建工具**: PyInstaller 6.14.2
- **Python版本**: 3.12.10

---

## ✨ 新增功能

### 🔧 字段类型提取功能

表字段导出工具新增字段类型提取功能，从Excel表格第6行提取字段类型信息。

#### 功能详情

1. **字段类型提取**
   - 从物理行第6行提取字段类型
   - 支持的类型：策划、前端、后端、前后端等
   - 自动检测并警告字段类型为空的情况

2. **JSON格式优化**
   - `fields_with_examples` 从"字段名,示例值"改为"字段名,字段类型"
   - 输出格式：`["des_cn,策划", "des_vcn,前端", "des_en,后端"]`

3. **Excel表格结构**
   ```
   行号 | 说明
   -----|------
   第5行 | 字段名（如：des_cn, des_vcn）
   第6行 | 字段类型（如：策划、前端、后端、前后端）
   第7行+ | 数据内容
   ```

---

## 📊 输出格式示例

### JSON格式

```json
{
  "no_text_tables": [
    {
      "table_name": "配置表.xlsx",
      "sheet_name": "系统配置"
    }
  ],
  "text_tables": [
    {
      "table_name": "npc_config.xlsx",
      "sheet_name": "npc_list",
      "fields_with_examples": [
        "des_cn,策划",
        "des_vcn,前端",
        "des_en,后端"
      ],
      "field_count": 3
    }
  ]
}
```

---

## 🔄 与上一版本的区别

### v1.27.2 → v1.28.0

| 项目 | v1.27.2 | v1.28.0 |
|------|---------|---------|
| **提取内容** | 字段名 + 示例值（第7行） | 字段名 + 字段类型（第6行） |
| **输出示例** | `des_cn,张若` | `des_cn,策划` |
| **警告信息** | 示例数据为空 | 字段类型为空 |
| **扫描范围** | 两个 c_ 标记之间 | 两个 c_ 标记之间 |

---

## ⚙️ 保持不变的功能

- ✅ **扫描范围**: 仅扫描两个 `c_` 标记之间的列
- ✅ **文本检测**: 从第7行开始检测数据内容
- ✅ **字段过滤**: 自动过滤 name、model、id、code、type
- ✅ **多语言支持**: 中文、越南文、泰文
- ✅ **多格式输出**: JSON、CSV、Excel

---

## 📝 使用说明

### 启动方式

直接运行：
```
dist\gametools_v1.28.0.exe
```

### Excel表格要求

确保Excel表格包含以下结构：

```
行1-4: 表头说明（可选）
行5: 字段名（必需）  → c_, des_cn, des_vcn, des_en, name, model, c_
行6: 字段类型（必需）→ 空, 策划, 前端, 后端, 前后端, 策划, 空
行7+: 数据内容
```

### 字段类型说明

| 类型 | 说明 | 用途 |
|------|------|------|
| **策划** | 策划专用字段 | 供策划人员编辑和维护 |
| **前端** | 前端客户端使用 | 客户端显示和逻辑 |
| **后端** | 后端服务器使用 | 服务器逻辑和数据 |
| **前后端** | 前后端共用 | 客户端和服务器都需要 |

---

## ⚠️ 注意事项

1. **字段类型为空**
   - 第6行字段类型为空时会生成警告
   - 输出格式为：`"字段名,"`

2. **标记要求**
   - 必须有两个 `c_` 标记
   - 只扫描标记之间的列
   - 未找到标记会跳过该工作表

3. **字段过滤**
   - name、model、id、code、type 字段会被自动过滤
   - 这些字段通常包含代码标识符，不需要翻译

---

## 🧪 测试验证

### 测试文件

项目包含测试文件和脚本：

```bash
# 创建测试Excel文件
python create_test_field_type_excel.py

# 运行测试
python test_json_format.py
```

### 测试覆盖

- ✅ 正常字段类型提取
- ✅ 空字段类型警告
- ✅ 多工作表处理
- ✅ 字段过滤功能
- ✅ JSON格式输出

---

## 📦 打包信息

### 构建环境

- **操作系统**: Windows
- **Python版本**: 3.12.10
- **PyInstaller**: 6.14.2
- **构建脚本**: gui/build_unified.py

### 文件结构

```
dist/
└── gametools_v1.28.0.exe    # 主程序（38.94 MB）
```

---

## 🔧 技术细节

### 代码修改

**修改文件**: `core/excel_field_extractor.py`

主要变更：
1. 从第6行提取字段类型（原为第7行示例数据）
2. 变量名从 `field_with_examples` 改为 `field_with_types`
3. 警告信息从"示例数据为空"改为"字段类型为空"
4. 更新相关注释和文档

### 测试文件

- `create_test_field_type_excel.py`: 创建测试Excel文件
- `test_json_format.py`: 测试JSON格式输出
- `docs/FIELD_TYPE_EXTRACTION_UPDATE.md`: 详细更新说明

---

## 📖 相关文档

- **快速入门**: docs/FIELD_EXTRACTOR_QUICKSTART.md
- **完整说明**: docs/EXCEL_FIELD_EXTRACTOR_README.md
- **更新说明**: docs/FIELD_TYPE_EXTRACTION_UPDATE.md
- **字段过滤**: docs/FIELD_FILTER_GUIDE.md

---

## 🐛 已知问题

暂无

---

## 📞 反馈与支持

如遇问题或有改进建议，请通过以下方式反馈：
- 项目 Issues
- 技术文档

---

## 📅 下一版本计划

- CSV 和 Excel 格式同步更新字段类型显示
- 字段类型的自定义配置
- 更多字段类型的预定义选项

---

## 🎉 致谢

感谢所有使用和反馈的用户！

---

**版本**: v1.28.0  
**发布日期**: 2025年11月20日  
**文件**: gametools_v1.28.0.exe
